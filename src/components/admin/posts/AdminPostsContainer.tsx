import * as React from 'react';

type AdminPostsContainerProps = Readonly<{
}>;

export const AdminPostsContainer = (props: AdminPostsContainerProps) => {
    console.log('admin post container');
    return (
        <div>admin post container</div>
    );
};
